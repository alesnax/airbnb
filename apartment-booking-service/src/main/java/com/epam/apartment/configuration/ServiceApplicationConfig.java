package com.epam.apartment.configuration;

/*import javax.sql.DataSource;

import org.apache.commons.dbcp.BasicDataSource;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.ImportResource;
import org.springframework.context.annotation.PropertySource;
import org.springframework.core.env.Environment;

@Configuration
@ComponentScan({ "com.epam.apartment.*" })
@PropertySource(value = { "classpath:jdbc.properties" })
*/
public class ServiceApplicationConfig {

	/*
	 * @Autowired private Environment environment;
	 * 
	 * @Bean public DataSource dataSource() { BasicDataSource dataSource = new
	 * BasicDataSource();
	 * dataSource.setDriverClassName(environment.getRequiredProperty(
	 * "jdbc.driverClassName"));
	 * dataSource.setUrl(environment.getRequiredProperty("jdbc.url"));
	 * dataSource.setUsername(environment.getRequiredProperty("jdbc.username"));
	 * dataSource.setPassword(environment.getRequiredProperty("jdbc.password"));
	 * 
	 * dataSource.setInitialSize(new
	 * Integer(environment.getRequiredProperty("jdbc.initialSize")));
	 * dataSource.setMaxActive(new
	 * Integer(environment.getRequiredProperty("jdbc.maxActive")));
	 * 
	 * return dataSource; }
	 */

}
